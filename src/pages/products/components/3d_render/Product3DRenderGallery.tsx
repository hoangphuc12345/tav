/**
 * Product3DRenderGallery
 *
 * The gallery is now rendered inline inside 3d_render/index.tsx
 * using the shared masonry pattern from products.css.
 *
 * This file is retained to avoid breaking any imports that may reference it.
 * It simply re-exports a no-op component.
 */

const Product3DRenderGallery = (_props: { filter: any[] }) => null;
export default Product3DRenderGallery;
